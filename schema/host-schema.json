{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "host_iso_dir": {
      "type": "string",
      "description": "The root directory path to locate distro ISO files."
    },
    "host_output_dir": {
      "type": "string",
      "description": "The directory path on the host where output is stored."
    }
  },
  "required": ["host_output_dir"],
  "additionalProperties": false
}
